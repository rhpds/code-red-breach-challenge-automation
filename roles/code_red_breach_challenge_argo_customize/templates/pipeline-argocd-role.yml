apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pipeline-argocd-role
rules:
  - apiGroups:
      - argoproj.io
    resources:
      - applications
    verbs:
      - get
      - list
      - watch
