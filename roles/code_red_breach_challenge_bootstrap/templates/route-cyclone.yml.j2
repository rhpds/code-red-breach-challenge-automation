kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: cyclonedx-bom-repo-server
  namespace: {{ code_red_breach_challenge_bootstrap_cyclonedx_namespace }}
spec:
  to:
    kind: Service
    name: cyclonedx-bom-repo-server
    weight: 100
  port:
    targetPort: http-listener
  tls:
    termination: edge
  wildcardPolicy: None